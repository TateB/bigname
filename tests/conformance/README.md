# Conformance Harness

This directory is reserved for the TypeScript conformance harness called for in ADR 0001 and `docs/workstreams.md`.

The bootstrap change only establishes the repository shape and leaves consumer-capability fixtures and contract checks for the dedicated conformance workstream.
